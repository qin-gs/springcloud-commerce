
spring:
  application:
    name: kafka-learn
  profiles:
    active: dev

# 暴露端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
